{
  "name": "aiSkill",
  "build": { "dockerfile": "Dockerfile" },
  "containerUser": "root",
  "initializeCommand": "bash .devcontainer/initializeHost.sh",
  "runArgs": [
    "--privileged",
    "--device",
    "/dev/fuse",
    "--hostname=aiSkill",
    "--dns=192.168.25.1",
    "--dns=9.9.9.9"
  ],

  "mounts": [
    "source=${localEnv:HOME}/.ssh/ssh-auth-sock,target=/ssh-agent,type=bind",
    "source=${localEnv:HOME}/.agents,target=/root/.agents,type=bind,readonly",
    "source=${localEnv:HOME}/.agent-devcontainer/pi/agent,target=/root/.pi/agent,type=bind"
  ],

  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "svelte.svelte-vscode",
        "esbenp.prettier-vscode",
        "streetsidesoftware.code-spell-checker",
        "dbaeumer.vscode-eslint",
        "ms-vscode.vscode-typescript-next"
      ]
    }
  },
  "postCreateCommand": "bash .devcontainer/postCreateScript.sh"
}
